# 核心依赖
crawl4ai>=0.6.0

# Web服务器依赖
flask>=2.0.0
flask-cors>=4.0.0
flask-socketio>=5.0.0

# 增强导航功能依赖
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 可选依赖（用于 LLM 提取策略）
openai>=1.0.0
anthropic>=0.20.0

# 其他依赖
aiohttp>=3.8.0 